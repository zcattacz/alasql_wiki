# Keyword `APPLY`

Is used as part of both `CROSS APPLY` and `OUTER APPLY`


```sql
    SELECT ... CROSS APPLY ...
    SELECT ... OUTER APPLY ...
```

Pleae see 

- [CROSS APPLY](Cross Apply)
- [OUTER APPLY](Outer Apply)